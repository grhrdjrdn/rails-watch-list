<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <div class="menu">
        <%= active_link_to 'Movie lists', lists_path, active: :exclusive %>
        <%= active_link_to 'Create a list', new_list_path, active: :exclusive %>
    </div>

    <div class="content">
      <%= yield %>
    </div>

  </body>
</html>
