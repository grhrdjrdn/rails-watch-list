<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>


    <div class="menu">
      <% if !current_page?(lists_path) && !current_page?('/') %>
        <%= link_to 'Show lists', lists_path %>
      <% end %>
      <% if !current_page?(new_list_path) %>
        <%= link_to 'Create a list', new_list_path %>
      <% end %>
    </div>

    <div class="content">
      <%= yield %>
    </div>

  </body>
</html>
